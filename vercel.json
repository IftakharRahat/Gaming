{
  "framework": "vite",
  "buildCommand": "npm run build:vercel",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/game/:path*",
      "destination": "/api/game/:path*"
    }
  ]
}